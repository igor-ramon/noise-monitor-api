version: "3.9"

services:
  noise-monitor-api:
    build: .
    container_name: noise-monitor-api
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
      CORS_ORIGIN: "*"
    volumes:
      - recordings_data:/app/recordings
    restart: always

volumes:
  recordings_data:
